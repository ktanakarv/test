name: Tets

on:
  push:
    paths:
        - '.circleci/**.txt'
    branches-ignore:
        - 'master'
        - 'main'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Check
        run: |
          . test.sh